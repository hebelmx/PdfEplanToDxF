# Decision Log — PLC → mini-EPLAN, Phase 2 planning

Canonical memory / audit trail for the BMAD planning chain (brief → PRD → epics).

## 2026-06-14

- **Artifact scope:** Abel chose the FULL CHAIN (Product Brief → PRD → Epics/Stories)
  to capture the Phase-2 business case so dev agents can be delegated to with full
  context.
- **Location:** placed under `docs/planning/` (not the BMAD default
  `{output_folder}/planning-artifacts/briefs/<run-folder>/`). Reasons: (a) Abel asked
  for the repo's docs/ area; (b) the installed BMAD config left `{output_folder}`
  unresolved as a literal folder name — avoid it; (c) keep brief/prd/epics together
  and findable for the chain. Each downstream skill is pointed at the prior artifact
  explicitly.
- **Working mode:** Fast path. Abel (expert; orchestrator is the human-in-the-loop)
  supplied the full business case up front; brief drafted directly with `[ASSUMPTION]`
  tags rather than section-by-section elicitation.
- **Brief shape:** added "Where We Are Today" (next-phase brief for a shipped product)
  and "Key Risks & Mitigations" (the Siemens export-completeness risk is material);
  otherwise followed the BMAD brief template.
- **Open `[ASSUMPTION]`s for Abel to confirm:** (1) the specific near-term Siemens
  project/timeline driving demand; (2) Phase-2 horizon ≈ next few weeks paced to when
  the first Siemens diagrams are needed.
- **Siemens import constraints (Abel, 2026-06-14) — resolves the two `[ASSUMPTION]`s:**
  - **TIA Openness is RULED OUT** (not viable in Exxerpro's environment). All Siemens
    input must come from **GUI file exports only**. This removes the "needs TIA
    installed at runtime" path entirely.
  - **S7-300 export path:** symbol-table / I-O-list export (tag↔address↔comment) + a
    hardware-config **PDF**. The PDF is human-read to hand-curate a Siemens module map —
    same pattern as the existing `module_db` (curated from vendor docs), so it fits.
  - **1200/1500 export path:** PLC tag-table XML + a GUI hardware export (hypothesis:
    CAx/AML) — the spike confirms completeness; the tag XML is the known-good part.
  - **Samples → `Fixtures/`** (gitignored plant data, never committed); gathering in
    progress, slowed by sluggish TIA-on-VM. VM optimization is a separate session.
  - **Timeline is sample-gated and soft**, not date-gated. Vendor-independent
    quick-wins proceed on Rockwell meanwhile.
- **Chain produced (2026-06-14):** `brief.md` → `prd.md` (FR-1…FR-14, guardrails as
  NFRs, risk register, FR→epic map) → `epics.md` (E1–E5, stories with Given/When/Then
  acceptance baking in the floor gate + never-invent + hygiene; dependencies + sample
  gating marked). No UX/Architecture docs — CLI/backend tool; architecture context is
  `docs/logix-to-qet-guide.md`. Epic shape E1–E5 accepted as proposed.
- **Open questions to confirm with Abel (carried in prd.md §11):** (1) which Siemens
  platform/project is the FIRST real fixture (sequences S7-300 vs 1200/1500); (2) whether
  the first TIA export carries a complete hardware list without Openness (FR-8 answers);
  (3) power-section config location — extend `project_template.json` vs a new
  `power_template.json` sibling; (4) Profinet-specific topology now vs generic comms tree.
- **Carried-forward product decisions (from the Phase-1 session, not re-litigated):**
  never invent data (TBD→__, generic fallback); Python 3.10+ stdlib only;
  language-agnostic JSON databases; public-repo hygiene (no plant data / *.L5X / *.qet
  / *_bom.csv / *.pdf committed); LLM authors CONFIG not QET code; one tested builder
  per folio with floor/positional tests; WADDING_1 floor (10/106/75/0) never regresses;
  demand-driven scope.

## 2026-06-16 — Epic 4 execution (Siemens TIA S7-1200/1500) + floor re-baseline

Execution decisions made on branch `feat/e4-tia-1200` (the orchestrator is the
human-in-the-loop; calls gated with Abel where they were genuine forks). Live
status: `docs/TIA-tracker.md` + GitHub issue #2.

- **Resolves the 4 open questions carried in prd §11 / 2026-06-14 above:**
  1. **First real Siemens fixture / platform = S7-1200 FIRST** — the real machine
     `Fixtures/Siemens/TiaPortal/IMV1_QRO001_*` (Q100 station), NOT the synthetic
     `S71200/Project1`. (Abel, 2026-06-16: the 1500 CAx is being re-exported "ca02",
     so the 1200 path leads. **Correction surfaced during TIA-3:** the `.aml` is the
     FULL plant and the Q100 CPU is actually a `CPU 1512SP F-1 PN` 1500-class F-CPU —
     so this "1200 floor machine" is really a 1500-class ET200SP system; both a 1214C
     and the 1512SP exist in the file.)
  2. **TIA export carries a complete hardware list WITHOUT Openness — YES (FR-8
     confirmed).** The CAx/AML GUI export (`*.aml`) holds the full plant (91
     station/module entries) with order numbers (`6ES7…`) + PROFINET `NetworkAddress`
     + `SubnetMask` + slot `PositionNumber`; `*_IO_Channels.xml` is the point source
     (tag↔address; empty `<Tag>` = spare). No runtime TIA install needed. The
     `PLCTags*.xlsx` Comment column is empty for the 1200 → descriptions stay "".
  3. **Power-section config location — STILL OPEN** (deferred to Story 5.1 / ALIM,
     blocked on Abel's panel power data; never-invent forbids guessing breakers/feeders).
  4. **PROFINET topology vs generic comms tree — BOTH, vendor-specific.** Built a
     Siemens-specific PROFINET network folio (`build_network_folio`, gated by IR
     `network_nodes`, 35 nodes on 192.168.10.x); Rockwell keeps its generic comms-tree
     topology folio. Subnet label read from the REAL `SubnetMask` (never synthesized);
     controllers flagged from parsed `DeviceItemType=CPU` (never a hard-coded host).

- **WADDING_1 floor RE-BASELINED (CHAN, 2026-06-16, Abel-eyeballed):**
  **10/106/75/0, 62 RESERVA, 33 folios → 11/106/75/0, 78 RESERVA, 35 folios.** Reason:
  the CHAN decision draws EVERY card channel as a box I/O point (mapped + RESERVA stub),
  both vendors, so all-spare cards/halves now emit a folio. **`matched=75` and
  `false-positives=0` are the never-move invariants; `drawn=106` (mapped only) also
  holds** — spares are counted separately and never inflate the matched floor. This
  re-baseline supersedes the `10/106/75/0` figure in the carried-forward decisions
  above and in epics.md NFR-6 (reconciled there).

- **NFR-5 hardening (FP=0 real counter, both pipelines)** — the floor tests asserted
  only the matched TOTAL, so a semantic mis-classification (right count, wrong type — a
  true false positive) shipped green. Now the floor tests assert the EXACT per-type
  breakdown parsed from the stderr `symbols` line (committed `2d2de39`).

- **Architecture / scope decisions taken during E4:**
  - **CLI = a SEPARATE command** `src/tia_to_qet.py` for the Siemens path, sharing the
    `plc_ir.PlcProject` seam + the extracted `render_project(ir, …, emit_vendor_folios)`;
    `logix_to_qet.py` stays Rockwell/`.L5X`. (Rockwell output verified byte-equivalent
    across the refactor.)
  - **Story 4.1 — `.aml`-DIRECT catalog, NOT a curated `module_db` JSON.** The CAx
    export already carries the real order numbers, so they are read directly per module
    (joined by physical name) rather than hand-curating a Siemens `module_db`. Deviation
    from the AC as written (which assumed the Rockwell `module_db` pattern); curation is
    unnecessary when the GUI export is authoritative. No match → catalog ''/addr None
    (never invented); pins stay `TBD`.
  - **Stories 2.2 (index) + 2.3 (rack) shipped SIEMENS-FIRST**, gated by
    `source_vendor=="siemens"`, additive (Rockwell output untouched). The plan framed
    these as vendor-independent; making them Rockwell-too is **pending** (Abel leaned
    "check whether the same need/defects exist on Rockwell first" rather than force it —
    revisit at the merge gate). Tracked in issue #2.
  - **Siemens symbol vocabulary:** only `push_button` matches the IMV1 tag vocabulary
    today (never-invent — the other Siemens tags have no confident symbol). A Siemens
    symbol dictionary is a future low-priority item, not a bug.
  - **Rockwell-pipeline audit** (Abel-requested) confirmed real analogues of the TIA
    defect classes (a `24V` rail no card references; `PV` 2-letter HMI substring;
    comms-bridge family gaps). These CHANGE validated Rockwell output → deferred behind
    Abel's desktop eyeball (issue #2), NOT yet fixed.

- **Delivery posture:** branch `feat/e4-tia-1200` feature-complete (TIA-1/2/3 + CHAN +
  NET + RACK+IDX + TIA-FIX-1 + FP-FIX); 374 tests green; pending Abel's desktop visual
  sign-off then the human merge gate → `main`. ALIM (Story 5.1/5.2) blocked on power data.
